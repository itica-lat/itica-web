name: Deploy

on:
  push:
    branches: [main]

concurrency:
  group: deploy-itica-web
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: self-hosted
    timeout-minutes: 15
    steps:
      - name: Deploy
        run: bash /home/hermes/deploy/itica-web.sh